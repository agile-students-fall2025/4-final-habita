# MongoDB connection string; replace placeholders with your own values
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@habita.whv6kme.mongodb.net/?appName=habita

# Secret used to sign JWTs (use a long random string)
JWT_SECRET=change_me

# Optional overrides
PORT=4000
JWT_EXP_DAYS=7

# Front-end origin allowed for CORS (e.g., React dev server)
CORS_ORIGIN=http://localhost:3000/
